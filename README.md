# Ansible playbooks to deploy Event Metadata System

(WIP)

To deploy EMS:
1) Edit `hosts` file...

2)
```
python3 -m venv env
source env/bin/activate
pip install ansible

ansible-playbook deploy-pgsql.pb.yml

```

